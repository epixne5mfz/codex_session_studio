# 你的 Codex 聊天记录，值得一个更好的家 —— Codex 合并台

> 一款纯本地、开箱即用的 macOS 小工具，让散落各处的 Codex 会话记录一键迁移、归档、找回。

## 为什么需要它？

如果你是 Codex（ChatGPT 桌面版编程助手）的重度用户，大概率遇到过这些烦恼：

- 换电脑、重装系统、切换中转账号后，**历史会话找不回来了**；
- `~/.codex` 备份了好几份，**哪份是最新的、里面有什么，完全是黑盒**；
- 想删掉一批过时会话给侧栏减负，又**怕误删重要记录**；
- 想找某次对话里的一段代码，**只记得内容、不记得标题**。

Codex 合并台就是为解决这些问题而生的。

## 它能做什么？

**一键迁移历史会话。** 自动扫描本机所有 Codex 数据目录（包括历史备份），把旧会话安全合并进当前正在使用的 Codex——已存在的只更新不重复，还能自动归入侧栏指定文件夹。

**全文搜索 + 精准筛选。** 不止搜标题，打开「搜内容」开关还能直接搜聊天正文，命中片段高亮展示；支持按日期范围、中转渠道、新增/已存在/已归档多维过滤，并可按日期、标题、中转灵活排序。

**批量管理不手抖。** 分组一键全选，勾选后实时显示「新增 X 条、更新 Y 条」；迁移前弹出差异预览清单，看清楚每一条再动手。

**三重安全兜底。** 每次写操作前自动备份数据库；删除的会话先进回收站可随时还原；内置备份管理界面，备份文件可一键还原或清理，出问题随时「撤销本次操作」。

**纯本地运行，隐私无忧。** 所有数据不出你的电脑，服务只监听 127.0.0.1，没有任何云端上传。

## 怎么用？

原生 Swift 桌面应用，DMG 安装包拖进「应用程序」即可，Intel 和 Apple Silicon 芯片通吃（macOS 11+）。双击打开，选好「来源」和「迁移到」，点「开始迁移」，确认预览清单——完成后一键重启 Codex，历史会话回到侧栏。

三步，找回你所有的 AI 结对编程记忆。

---

*如果这个工具帮到了你，欢迎点赞、在看、转发给同样被 Codex 记录管理折磨的朋友。*
